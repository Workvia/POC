import styled from '@emotion/styled';

const StyledContainer = styled.div`
  padding: ${({ theme }) => theme.spacing(4)} 0;
`;

export const TemplateSettingsTab = () => {
  return (
    <StyledContainer>
      <p>Template settings coming soon</p>
    </StyledContainer>
  );
};
